[pytest]
asyncio_mode = auto
testpaths = tests
# The app is imported as `core.*` / `components.*` from the repository root, so
# the root must be on the path — `pythonpath` does that without a PYTHONPATH in
# every command and without an installable package.
pythonpath = .
