# Production: the real EPICS network (EPICS_BACKEND=aioca).
#
# Same pin as the gateway this app replaces
# (backend/python-websocket-server/requirements.txt), so a station that talked
# to Channel Access through the old service talks to it through this one with
# the same client library.
-r requirements.txt
aioca==2.0
